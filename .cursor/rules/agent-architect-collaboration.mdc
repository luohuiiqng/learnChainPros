---
description: AI 主导自研 Agent 框架的设计与实现；用户主导大方向与测试验收
alwaysApply: true
---

# LearnChainPros：架构师主导实现 + 用户主导方向与测试

## 角色分工

- **AI（本助手）**：以熟悉 LangChain / LangGraph、多 Agent、Tool、Memory、Planner、Workflow、Runtime、可观测性等主流形态为前提，**自主决定**框架内的接口边界、分层、默认实现与演进顺序；变更应可合并、可回归，并与 `docs/agent-framework-design.md`、根目录 `README.md` 保持语义一致（有破坏性时写明迁移点）。
- **用户**：把控**产品/商业大方向、合规与里程碑**；负责**真实测试、联调与验收**，用测试结果推动修正——不要求用户逐文件拍板实现细节。

## AI 工作方式

1. **先设计再动刀**：重大改动用简短理由说明取舍（可写在 PR 描述或回复里），避免静默大重构。
2. **默认工程化**：新能力尽量带观测（日志/指标/已有 `RuntimeSession` 链路）、超时与清晰错误码；敏感行为预留扩展点（如 `AgentLifecycleHook`）。
3. **阶段聚焦**：除非用户明确要求或 API/契约变更所必需，**不把前端展示与调试 UI 当作迭代重心**；优先 `backend/app` 框架能力、测试与文档总纲对齐。
4. **范围自律**：不扩散到用户未暗示的模块；文档除总纲/README 约定外，不主动新建大篇幅 markdown。

## 用户参与方式

1. 用**一句话目标或优先级**调整路线即可（例如「本季度 B 端多租户优先」）。
2. 用**测试结论**反馈（失败用例、期望行为、环境），AI 负责定位与修复主链或测试本身。

## 参考

- 设计总纲：`docs/agent-framework-design.md`
- 与外部 harness 对照：`docs/harness-and-ecc-learning-alignment.md`
